/// Optional public URLs for community / help (leave empty to hide in-app links).
class CommunityLinks {
  CommunityLinks._();

  /// e.g. https://example.com/guidelines — empty hides the button on guidelines page.
  static const String guidelinesUrl = '';
}
